const palabras = ['spray', 'élite', 'exuberante', 'destrución', 'presente'];

const resultado = palabras.filter((palabra) => palabra.length > 6);

console.log(resultado);
// Expected output: Array ["exuberant", "destruction", "present"]
